let
    var n := 0     /* o numero    */
    var f := 1     /* o fatorial  */
    var i := 2     /* um contador */

in

	if (n>0) then  (
		n:=0;
	) else (
		if (-1) then (
			f:=n;
		);
		n:=9000; /* retorna valor de mesmo tipo que o do if then */
	);
end
