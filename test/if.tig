let
    var n := 0     /* o numero    */
    var f := 1     /* o fatorial  */
    var i := 2     /* um contador */
    
in

	if (f|i&n) then (
	
		n:=-n;		/* if then simples nao retorna valor */
	);
end
